
	// "github.com/unidoc/unioffice/document"

	doc, err := document.Open("Locomotion Planning, Optimization and Control Design for Humanoid Robot.docx")

	if err != nil {
		log.Fatalf("error opening document: %s", err)
	}

	for _, para := range doc.Paragraphs() {
		for _, run := range para.Runs() {
			fmt.Print(run.Text())
		}
		fmt.Println()
	}